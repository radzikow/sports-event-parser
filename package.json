{
  "name": "sports-event-parser",
  "version": "1.0.0",
  "description": "Task: you have to refactor code in task/app.js file. Change it, split it, improve it, do it your own way! You can use version of node you prefer. Code was tested in node 6, 10, 12, 14, 16, so it should be quite version agnostic at this stage. Application should print out the array of parsed events.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "compile": "tsc",
    "start": "node dist/main.js",
    "start:dev": "nodemon",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.8",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
